Iniciar Backend desde contenedor:

1. abrir Docker desktop en Windows
2. Para verificar Docker ejecutar: 

	docker --version

3. Luego de confirmar que esta corriendo Docker


- Si no hay cambios ejecutar:

	docker-compose up

- ejecutar si se ha realizado cambios en el contenedor:

	docker-compose up –build